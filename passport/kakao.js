const localStrategy = require('passport-local').Strategy;
const bcrypt = require('bycrypt');
const { } = require('../modules/mysql-conn') = require('../modules/mysql-conn');


module.exports = (passport) =>{
	
};